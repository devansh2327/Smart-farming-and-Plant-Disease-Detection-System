import { useState } from 'react'
const apiBaseUrl = import.meta.env.VITE_API_BASE_URL || 'http://localhost:8080'
const initialValues = { nitrogen: 90, phosphorus: 42, potassium: 43, temperature: 20.87974371, humidity: 82.00274423, ph: 6.502985292, rainfall: 202.9355362 }
export default function CropRecommendation() { const [values, setValues] = useState(initialValues); const [result, setResult] = useState(''); const [error, setError] = useState(''); async function submit(event) { event.preventDefault(); setResult(''); setError(''); try { const response = await fetch(`${apiBaseUrl}/api/ml/crop-recommendation`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(values) }); const data = await response.json(); if (!response.ok) throw new Error(data.message || data.detail || 'Prediction failed'); setResult(data.crop) } catch (requestError) { setError(requestError.message) } } return <main className="crop-page page-shell"><section className="page-hero crop-hero"><p className="eyebrow">Crop intelligence</p><h1>Choose the right crop, confidently.</h1><p>Combine soil nutrients and local growing conditions for a smart recommendation.</p></section><section className="tool-panel"><div className="tool-panel-intro"><span>🌾</span><div><h2>Soil & climate profile</h2><p>Enter the latest values from your field test.</p></div></div><form onSubmit={submit} className="smart-form crop-form">{Object.entries(values).map(([name, value]) => <label key={name}>{name.replace(/([A-Z])/g, ' $1')}<input required type="number" step="any" value={value} onChange={(event) => setValues({ ...values, [name]: Number(event.target.value) })} /></label>)}<button type="submit">Get recommendation <span>→</span></button></form></section>{result && <section className="result-spotlight"><p className="eyebrow">Recommended crop</p><h2>{result}</h2><p>Based on your field’s current nutrient and weather profile.</p></section>}{error && <p className="error">{error}</p>}</main> }
